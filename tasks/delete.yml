---
- name: Delete | import common tasks
  tags:
    - memcached
    - pbos-infra
  ansible.builtin.import_tasks: "common.yml"

- name: Delete | purge packages
  tags:
    - memcached
    - pbos-infra
  ansible.builtin.apt:
    name: "{{ item }}"
    autoremove: true
    purge: true
    state: absent
  loop: "{{ packages }}"
  become: true
...
